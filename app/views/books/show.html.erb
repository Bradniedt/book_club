<div class="">
  <%= link_to "Add Review", new_book_review_path(@book) %>
</div>
<%= @book.title %>
<%= @book.pages %>
<%= @book.author %>
 <% @book.reviews.each do |review|%>
  <%= review.title %>
  <%= review.description %>
  <%= review.rating %>
 <% end  %>
